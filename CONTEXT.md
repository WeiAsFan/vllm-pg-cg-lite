# PG-CG Lite 项目语境

本语境统一 PG-CG Lite 的项目定位、执行环境、实验角色和证据口径，使开发、结果分析、简历描述与面试表达使用同一套语言。

## 共同语言

### 项目定位

**PG-CG Lite**：
一个带有小型离线优化算法的 vLLM 推理部署与性能工程项目，用于研究工作负载感知的 CUDA Graph capture-size 剪枝及其启动成本与稳态性能权衡。
_避免：新推理引擎、通用推理加速方案、新 CUDA Graph 算法_

**项目成功**：
结果首先必须正确、可追溯且具有因果解释，其次才评价工程收益和面试表达；满足前者的负结果同样属于项目成果。
_避免：跑出正向数字、达到预设提升_

**范围内结论**：
只对实际验证过的硬件、模型、vLLM 版本和工作负载成立的受限判断，不自动推广到其他部署环境。
_避免：通用结论、生产级保证_

**面试可用版本**：
已经完成远端验证和正式实验、保留可追溯证据并明确结论边界，且能够在面试追问下解释关键选择与失败模式的项目状态。
_避免：代码完成、仓库可访问、得到一次正向结果_

**面试叙事**：
简历中的简短项目介绍及面试中的详细讲解与答疑；GitHub 仓库是证据载体，而不是主要展示方式。
_避免：仓库演示、照读 README_

**面试能力证据**：
能够共同证明推理部署、可观测性补齐、优化建模、实验设计、性能分析和结论约束能力的一组事实与材料。
_避免：代码量、功能数量、单个最好数字_

### 执行环境

**本地工作站**：
用于编辑、文档整理和 Git 管理的非运行环境；不具备项目所需的 GPU 与 PyTorch，不在此执行测试或性能验证。
_避免：本地实验环境、测试机_

**SSH 操作端**：
并非本机的另一台 Linux 设备；用户在该设备上执行 `ssh` 命令，登录远端实验服务器。
_避免：本机、远端实验服务器_

**远端实验服务器**：
由 SSH 操作端登录的小型服务器，是本项目实际运行以及执行测试和正式实验的唯一权威环境。
_避免：本机、SSH 操作端、生产环境_

**远端验证**：
在远端实验服务器的明确代码版本上执行固定门禁与实验，并保存环境、配置、命令、退出状态和原始输出的过程。
_避免：本机验证、口头确认、只看最终汇总_

### 优化对象与实验角色

**Capture size**：
vLLM 为 CUDA Graph 预先捕获并在运行时用于向上匹配 scheduled-token 数量的静态尺寸。
_避免：CUDA Graph 数量、batch size_

**默认集合**：
在给定服务配置下由 vLLM 解析出的完整 capture-size 集合，是所有合法剪枝候选的来源。
_避免：搜索空间、画像尺寸集合_

**合法子集**：
完全取自默认集合、保留默认最大尺寸且不超过尺寸预算的 capture-size 集合。
_避免：重新构造的端点、任意观测尺寸集合_

**工作负载画像**：
从真实服务运行中聚合得到的 scheduled-token 使用分布，只用于解释和选择 capture size，不代表通用流量分布。
_避免：训练数据、线上流量真值_

**默认全集（A 组）**：
使用 vLLM 默认集合的正式对照组，用于建立未经尺寸剪枝的基线。
_避免：未优化组、旧版本组_

**同预算规则子集（B 组）**：
不读取工作负载画像、但与画像子集使用相同尺寸数量的确定性规则子集，用于隔离“仅减少尺寸数量”的效果。
_避免：Lite 组、随机子集、弱基线_

**画像子集（C 组）**：
根据工作负载画像从默认集合中选择的合法子集，用于检验画像信息是否带来同预算增益。
_避免：B 组、任意八个尺寸、画像端点集合_

### 证据与结果口径

**历史预实验**：
在当前算法契约与正式协议冻结前产生的运行结果，只用于证明环境可行性、发现问题和形成后续假设，不进入当前正式结果表。
_避免：正式结果、最终基线_

**正式证据**：
由经过审查的版本按照冻结协议产生，并能追溯到代码版本、环境、配置、工作负载和原始输出的材料。
_避免：截图、手工摘录、无法定位来源的汇总数字_

**正式结果**：
通过全部远端门禁并由 A/B/C 正式证据支持的受限结论；结果可以为正、为负或呈现明确权衡。
_避免：单次运行结果、历史预实验、最好一次结果_

**配置正确性**：
规划结果满足合法子集契约、尺寸预算和确定性最优规则，并由单元测试及小规模穷举对照支持。
_避免：配置能够启动、JSON 能被解析_

**功能等价性**：
在受控的确定性正确性实验中，相同请求经过 A/B/C 配置得到完全一致的输出。
_避免：输出数量相同、文本看起来相似、性能运行间逐字相同_

**性能运行有效性**：
真实非确定性服务模式下的固定工作负载全部完成、无请求错误且 token 数量符合协议；运行间文本差异本身不作为失败条件。
_避免：功能等价性、单次吞吐成功_

**因果可解释性**：
通过 A/B/C 对照区分默认全集、尺寸数量减少和画像引导选择各自造成的影响。
_避免：Lite 比默认好、相关性即因果_

**正确结果**：
同时满足配置正确性、功能等价性、性能运行有效性、证据可追溯性和结论边界的结果。
_避免：指标符合预期、脚本显示 PASS_

**有意义结果**：
能够回答尺寸剪枝是否降低启动成本、画像是否提供同预算增益，以及为此付出了何种稳态代价；负结果只要能回答这些问题同样有意义。
_避免：所有指标都提升、证明方案普遍有效_
